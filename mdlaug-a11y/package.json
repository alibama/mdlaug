{
  "name": "mdlaug-repair",
  "version": "0.9.10",
  "private": true,
  "description": "Browser extension that repairs, re-presents, and audits digital libraries against the mDLAUG accessibility guidelines.",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "for f in extension/engine/*.js extension/src/*.js extension/ui/*.js; do node --check \"$f\" || exit 1; done && node -e \"JSON.parse(require('fs').readFileSync('extension/manifest.json'))\" && echo ok",
    "check:install": "bash extension/scripts/check-install.sh",
    "pack": "bash scripts/pack-extension.sh",
    "bdd": "node bdd/build.js"
  },
  "devDependencies": {
    "fake-indexeddb": "^6.0.0",
    "jsdom": "^25.0.0",
    "pdfjs-dist": "4.10.38"
  },
  "keywords": [
    "accessibility",
    "a11y",
    "wcag",
    "mdlaug",
    "digital-library",
    "screen-reader",
    "manifest-v3",
    "chrome-extension"
  ]
}